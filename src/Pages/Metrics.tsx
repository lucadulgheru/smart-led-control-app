import { Typography } from "@mui/material";
import Box from "@mui/material/Box";

const Metrics = () => {
    return (
        <Box>
            <Typography>METRICS PAGE</Typography>
        </Box>
    );
};

export default Metrics;